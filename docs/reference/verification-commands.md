# 검증 명령어 레퍼런스

## 📌 문서 관리 지침
**목적**: 검증 명령어 전체 목록 - 실행 방법, 예상 결과, 오류 해결 방법 종합 데이터
**대상**: 모든 AI (작업 완료 후 검증이 필요한 모든 경우)
**범위**: 명령어 사양과 결과만 포함 (사용 법칙이나 상세 가이드 없음)
**업데이트 기준**: package.json scripts 변경 시 자동 업데이트
**최대 길이**: 8000 토큰 (현재 약 6000 토큰)
**연관 문서**: [자동화 시스템](./automation-systems.md), [빠른 시작](../tutorial/01-quick-start.md)

## ⚠️ 금지사항
- 명령어 사용법이나 트러블슈팅 가이드 추가 금지 (→ how-to/ 문서로 이관)
- CI/CD 파이프라인 설정이나 도구 설명 추가 금지 (→ how-to/ 문서로 이관)
- 검증 전략이나 품질 철학 설명 추가 금지 (→ explanation/ 문서로 이관)

---

*프로젝트 품질 검증을 위한 모든 명령어와 예상 결과*

---

## 🚀 통합 검증 명령어

### 전체 시스템 검증 (추천)
```bash
# 최고 우선순위 - 모든 검증을 531ms에 완료
npm run verify:parallel

# 예상 결과:
# ✅ TypeScript compilation: passed (5ms)
# ✅ ESLint checks: passed (45ms) 
# ✅ Biome formatting: passed (23ms)
# ✅ Build verification: passed (421ms)
# ✅ Type generation: passed (28ms)
# ✅ Security checks: passed (9ms)
# 🎉 All verifications completed in 531ms
```

### 핵심 품질 검증
```bash
# TypeScript 타입 검증 (필수)
npm run types:check
# 예상: Found 0 errors. Watching for file changes.

# 빌드 검증 (배포 가능성)
npm run build  
# 예상: ✓ Compiled successfully

# 코드 스타일 검증
npx biome check .
# 예상: All checks passed!
```

---

## 📋 영역별 검증 명령어

### TypeScript 타입 검증
```bash
# 전체 프로젝트 타입 체크
npm run types:check
# 성공: Found 0 errors
# 실패: src/path/file.ts(10,5): error TS2322

# 특정 파일 타입 체크  
npx tsc --noEmit src/components/MyComponent.tsx
# any 타입 사용 금지 확인

# 타입 정의 생성 (DB 변경 후)
npm run types:generate
# 결과: Generated types from Supabase
```

### 코드 품질 검증
```bash
# ESLint 전체 검사
npm run lint
# 성공: ✨ 0 problems (0 errors, 0 warnings)

# Biome 포매팅 및 린트
npx biome check .
# 성공: All checks passed!

# Biome 자동 수정
npx biome check . --apply
# 수정된 파일 목록 표시
```

### 빌드 및 배포 검증
```bash
# Next.js 프로덕션 빌드
npm run build
# 성공 시: Route (pages) / Size / First Load JS
# 실패 시: 오류 메시지와 함께 중단

# 개발 서버 시작 (2.5초)
npm run dev
# 성공: Ready in 2.5s
# 실패: 포트 충돌 또는 설정 오류

# Vercel 배포 시뮬레이션
vercel build
# 성공: Build completed
```

---

## 🗄️ 데이터베이스 검증

### 테이블 및 RLS 정책 검증
```bash
# 서비스 역할로 RLS 정책 확인
node scripts/verify-with-service-role.js
# 성공: ✅ All RLS policies working correctly
# 실패: ❌ RLS policy missing for table: table_name

# 특정 테이블 구조 확인
node scripts/check-profiles-table.js
# 결과: Table structure and policies verified

# 모든 테이블 상태 검증
node scripts/check-yl-channels.js
# YouTube 관련 테이블 구조 확인
```

### 마이그레이션 검증
```bash
# SQL 파일 실행
node scripts/supabase-sql-executor.js --method pg --file migrations/001.sql
# 성공: ✅ SQL executed successfully
# 실패: ❌ PostgreSQL error with details

# 타입 생성 확인
wc -l src/types/database.generated.ts
# 정상: 1000줄 이상
# 비정상: 100줄 미만 (가짜 타입 가능성)

# 첫 5줄로 자동 생성 확인
head -5 src/types/database.generated.ts
# 예상: /** * This file was auto-generated by Supabase CLI */
```

---

## 🧪 테스트 검증

### 유닛 테스트
```bash
# 모든 테스트 실행
npm run test
# 성공: Tests: X passed, 0 failed
# 실패: 실패한 테스트와 스택트레이스

# 테스트 커버리지 (80% 목표)
npm run test:coverage
# 목표: Statements 80%+, Branches 80%+, Functions 80%+, Lines 80%+

# 감시 모드 (개발 중)
npm run test:watch
# 파일 변경 시 자동 재실행
```

### E2E 테스트
```bash
# Playwright E2E 테스트 (헤드리스)
npm run test:e2e
# 성공: All tests passed (X passed)
# 실패: 실패한 시나리오와 스크린샷

# UI 모드로 E2E 테스트 실행
npm run test:e2e:ui
# 브라우저에서 테스트 상황 시각적 확인

# 특정 테스트만 실행
npx playwright test auth.spec.ts
# 인증 관련 테스트만 실행
```

---

## 🔒 보안 검증

### 보안 정책 확인
```bash
# 보안 검사 스크립트
npm run security:test
# Zod 스키마, XSS 방지 등 검증

# 환경변수 보안 확인  
npm run check:env
# .env 파일의 보안 설정 검증

# 의존성 보안 검사
npm audit
# 성공: 0 vulnerabilities 
# 경고: X vulnerabilities (moderate: Y, high: Z)
```

### RLS 정책 동작 확인
```bash
# 실제 RLS 정책 테스트
curl -X GET "http://localhost:3000/api/users" -H "Authorization: Bearer invalid-token"
# 예상: {"error": "User not authenticated"}

# 인증된 요청 테스트  
curl -X GET "http://localhost:3000/api/users" -H "Authorization: Bearer valid-token"
# 예상: 사용자 데이터 반환 (자신의 데이터만)
```

---

## 📊 성능 검증

### 빌드 크기 분석
```bash
# Next.js 번들 분석
npm run analyze
# 번들 크기와 구성 요소 시각화

# Lighthouse 성능 측정
npx lighthouse http://localhost:3000
# Performance, Accessibility, Best Practices, SEO 점수

# 로딩 시간 측정
curl -w "@curl-format.txt" -s -o /dev/null http://localhost:3000
# 응답 시간, DNS 조회 시간 등 상세 측정
```

### 메모리 및 CPU 사용량
```bash
# 개발 서버 리소스 사용량
ps aux | grep next-server
# CPU 및 메모리 사용률 확인

# 빌드 시 메모리 사용량
NODE_OPTIONS="--max-old-space-size=4096" npm run build
# 메모리 부족 시 힙 크기 증가
```

---

## 🔧 자동화된 검증 스크립트

### 일일 상태 체크
```bash
# 프로젝트 건강도 종합 체크
node scripts/daily-tracker.js
# 성공률, 오류 통계, 성능 지표 리포트

# 자산 현황 스캔
node scripts/asset-scanner.js  
# 컴포넌트 96개, API 40개, 테이블 0개 등 현황

# 건강도 모니터링
node scripts/health-monitor.js
# 시스템 전반적인 건강 상태 점검
```

### CI/CD 파이프라인 검증
```bash
# GitHub Actions 로컬 실행
act push
# CI/CD 파이프라인 로컬에서 테스트

# Vercel 배포 전 검증
vercel --prod --confirm
# 프로덕션 배포 전 최종 검증
```

---

## ❌ 일반적인 오류와 해결책

### TypeScript 오류
```bash
# 오류: Cannot find module '@/types'
# 해결: 
npm run types:generate
# 또는 tsconfig.json 경로 확인

# 오류: Type 'any' is not assignable  
# 해결:
npx biome check src/**/*.ts
# any 타입 사용 위치 확인 후 수정
```

### 빌드 오류
```bash
# 오류: Module not found
# 해결:
npm install
# 의존성 재설치

# 오류: Environment variable not found
# 해결:
cp .env.example .env.local
# 환경변수 파일 생성 후 값 설정
```

### 데이터베이스 연결 오류
```bash
# 오류: Connection refused
# 해결:
# 1. Supabase 프로젝트 상태 확인
# 2. .env.local의 DATABASE_URL 확인
# 3. 네트워크 연결 상태 확인

# 오류: RLS policy violation
# 해결:
node scripts/verify-with-service-role.js
# RLS 정책 상태 확인 후 수정
```

---

## 📈 성공 기준

### 필수 통과 기준
- [ ] `npm run verify:parallel`: 531ms 내 완료
- [ ] `npm run types:check`: 0 errors
- [ ] `npm run build`: 성공적 컴파일
- [ ] `npm run test:coverage`: 80% 이상 커버리지

### 품질 기준  
- [ ] ESLint: 0 errors, 0 warnings
- [ ] Biome: All checks passed
- [ ] Security audit: 0 high/critical vulnerabilities
- [ ] Performance: Lighthouse 90+ 점수

### 운영 준비 기준
- [ ] E2E 테스트: 주요 플로우 100% 통과
- [ ] RLS 정책: 모든 테이블 적용
- [ ] 환경변수: 모든 필수 값 설정
- [ ] 배포: Vercel 성공적 배포

---

## 🔗 관련 스크립트

### package.json 스크립트 목록
```json
{
  "scripts": {
    "verify:parallel": "병렬 검증 (531ms)",
    "types:check": "TypeScript 타입 검증",
    "types:generate": "Supabase 타입 생성",
    "build": "Next.js 프로덕션 빌드",
    "dev": "개발 서버 시작",
    "test": "Jest 유닛 테스트",
    "test:coverage": "테스트 커버리지",
    "test:e2e": "Playwright E2E 테스트",
    "lint": "ESLint 검사",
    "security:test": "보안 검사"
  }
}
```

### 주요 검증 스크립트 위치
- `/scripts/verify-with-service-role.js` - RLS 정책 검증
- `/scripts/supabase-sql-executor.js` - SQL 실행
- `/scripts/daily-tracker.js` - 일일 상태 체크
- `/scripts/asset-scanner.js` - 자산 현황 스캔
- `/scripts/health-monitor.js` - 건강도 모니터링

---

**💡 권장사항**: 매일 `npm run verify:parallel`을 실행하여 프로젝트 상태를 확인하고, 코드 변경 후에는 해당 영역의 구체적인 검증 명령어를 실행하세요.