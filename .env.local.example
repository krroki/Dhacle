# ====================================
# Supabase Configuration (필수)
# ====================================
# Supabase 프로젝트 URL (https://app.supabase.com/project/[project-id]/settings/api)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (공개 가능한 키)
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key (서버 사이드 전용, 절대 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ====================================
# OAuth Configuration
# ====================================
# Kakao OAuth는 Supabase Dashboard에서 설정
# Authentication > Providers > Kakao에서 설정
# - Client ID와 Client Secret 입력
# - Redirect URL: https://[your-project].supabase.co/auth/v1/callback

# ====================================
# 사이트 설정
# ====================================
# Site URL - API 통신 및 리디렉션에 필요
# 개발: http://localhost:3000
# 프로덕션: https://dhacle.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ====================================
# 암호화 키 설정 (필수)
# ====================================
# API Key 암호화에 사용되는 키 (⚠️ 정확히 64자의 hex 문자열 필요)
# 생성 방법: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 
# ⚠️ 주의사항:
# 1. 반드시 64자여야 함 (63자나 65자는 에러 발생)
# 2. 이 키를 변경하면 기존에 저장된 모든 API Key를 다시 등록해야 합니다
# 3. 아래는 예시입니다. 실제 사용 시 새로 생성하세요
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ====================================
# YouTube API 설정 (변경됨)
# ====================================
# 이제 OAuth 시스템이 제거되었습니다.
# 사용자별 API Key는 웹사이트의 설정 페이지에서 개별적으로 등록합니다.
# 경로: /settings/api-keys
# 
# 각 사용자는 개인 Google Cloud Console에서 발급받은 
# YouTube Data API v3 Key를 사용하여 일일 10,000 units의 
# 독립적인 할당량을 갖게 됩니다.

# ====================================
# 토스페이먼츠 결제 설정 (필수)
# ====================================
# 토스페이먼츠 Dashboard: https://developers.tosspayments.com
# 테스트 키 사용 (test_ck_, test_sk_)
# 상용 키는 심사 후 발급됩니다
TOSS_SECRET_KEY=test_sk_0RnYX2w532DPKe7PNzWxrNeyqApQ
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_0RnYX2w532DPKe7PNzWxrNeyqApQ

# Webhook 서명 시크릿 (Webhook 엔드포인트 생성 후 발급)
# Dashboard > 개발자 센터 > Webhook에서 확인
TOSS_WEBHOOK_SECRET=your_webhook_secret_here

# ====================================
# Cloudflare Stream Configuration (선택)
# ====================================
# Cloudflare Dashboard: https://dash.cloudflare.com
# Stream 탭에서 활성화 후 설정
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_STREAM_TOKEN=your_stream_token
CLOUDFLARE_CUSTOMER_SUBDOMAIN=customer-xxxxx

# ====================================
# Additional Security Keys (필수)
# ====================================
# JWT 시크릿 키 (32자 이상)
# 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET_KEY=your-32-character-or-longer-random-string

# ====================================
# Optional Configuration
# ====================================

# 애널리틱스 (선택사항)
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_HOTJAR_ID=XXXXXXX

# ====================================
# 중요 안내사항
# ====================================
# 1. 이 파일을 복사하여 .env.local 파일을 생성하세요
# 2. 실제 값으로 교체하세요 (placeholder 값 사용 금지)
# 3. .env.local 파일은 절대 Git에 커밋하지 마세요
# 4. Vercel 배포 시 Environment Variables에 설정하세요
# 5. SUPABASE_SERVICE_ROLE_KEY는 서버 사이드에서만 사용하세요